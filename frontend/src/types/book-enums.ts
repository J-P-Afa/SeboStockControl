export enum EditionType {
  NORMAL = 'normal',
  VARIANTE = 'variante',
}

export enum Condition {
  NOVO = 'novo',
  USADO = 'usado',
}

export enum Status {
  COMPLETO = 'completo',
  EM_LANCAMENTO = 'em_lancamento',
}
